apiVersion: apps/v1
kind: Deployment
metadata:
  name: metrics-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: metrics-svc
  template:
    metadata:
      labels:
        app: metrics-svc
    spec:
      containers:
      - name: metrics-svc
        image: servantscode/metrics-svc
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        resources:
          limits:
            memory: "200Mi"
        env:
        - name: ES_JAVA_OPTS 
          value: "-Xmx128m -Xms128m"
---
kind: Service
apiVersion: v1
metadata:
  name: metrics-svc
spec:
  selector:
    app: metrics-svc
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
